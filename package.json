{
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.0.2",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.0"
  },
  "scripts": {
    "prepare": "cd .. && husky install front/.husky",
    "test": "lint-staged"
  },
  "lint-staged": {
    "*.{js,ts,css,sass,vue}": [
      "npm run lint"
    ]
  }
}
