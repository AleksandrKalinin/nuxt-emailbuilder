{
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.0.2",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "test": "lint-staged"
  },
  "lint-staged": {
    "*.{js,ts,css,sass,vue}": [
      "cd client && npm run lint"
    ]
  }
}
